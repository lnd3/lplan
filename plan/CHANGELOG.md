# Plan Changelog

Append-only record of all status and priority changes.

Format: `YYYY-MM-DD | ID | old_status → new_status | note`

---

2026-09-01 | A036 | NEW → DONE | priority_drivers made optional (default=[]) in Project model — was required+non-empty, causing parse failure and entities vanishing from all views when field omitted. Model-level validator removed; SchemaValidator still requires it non-empty, and now also validates present driver keys against the framework vocabulary. Schema docs and stale tests updated to match. Filed under P009 (originating fix was drive-by from TradeFlow).
2026-08-30 | D005 | NEW → PLANNING | Template File Family Scaling design — companion file convention for any template-instantiated file (_extension, _tmp, _learnings, _history, _whatever). Non-breaking; opt-in.
2026-08-30 | A025 | NEW → DONE | Consumer repo structural alignment check added to WORKFLOW_DETAILS.md § Framework Updates + WORKFLOW.md reference (policy change, explicitly flagged — not P009)
2026-08-30 | A024 | NEW → DONE | AI agent memory maintenance documented in WORKFLOW.md + template + TradeFlow plan/WORKFLOW.md (drive-by from TradeFlow)
2026-08-22 | P003 | IN_PROGRESS → DONE | Tier 3 Phase 3 complete: Gantt and Burndown chart visualizations built. Analytics dashboard now shows: metrics tables + Gantt timeline + Burndown progress. All visualizations SVG-based, zero external deps.

2026-08-22 | P001 | DONE → DONE | Tier 1 delivery validated and dogfooded

2026-08-22 | P002 | IN_PROGRESS → DONE | All Tier 2 Operational/Governance/Visibility features shipped. Web UI (plan serve) and additional scaffolding added beyond scope.

2026-08-24 | P004 | IN_PROGRESS → DONE | Web Server & UI Refactoring complete: monolithic server.py split into modular JS (editor, tree, preview, status modules) + Flask server.

2026-08-24 | P005 | IN_PROGRESS → DONE | Master Plans & Strategic Vision Architecture complete: MasterPlan entity (M-prefix) implemented with cross-repo distribution support.

2026-08-24 | P006 | IN_PROGRESS → DONE | Thesis Framework Integration complete: Thesis entity (T-prefix) implemented with conviction levels and many-to-many thesis↔master_plan relationships.

2026-08-24 | M001 | NEW → IN_PROGRESS | Developer Experience Excellence master plan created; work started under P004/P007.

2026-08-24 | M002 | NEW → PLANNING | Scalability Foundation master plan created; not yet started.

2026-08-24 | T001, T002, T003 | NEW → HELD | Founding theses captured (strategy/execution separation, schema-driven validation, web visualization accessibility).

2026-08-26 | C001–C005 | NEW → STABLE | Concept entity type (C-prefix) added to lplan; five core concepts extracted from the framework itself (hierarchical entity model, modular architecture, event delegation, dependency graph analysis, YAML frontmatter format).

2026-08-27 | — | catch-up | CHANGELOG synced to actual entity state per WORKFLOW.md upkeep pass — no new work performed, this closes the audit-trail gap for 2026-08-24 through 2026-08-26.

2026-08-27 | P009 | NEW → IN_PROGRESS | External Maintenance catch-all project created, plus new WORKFLOW.md § "External Contribution Workflow (Drive-By Fixes)". Reason: recent lplan-source fixes were made by agents whose active context was an external repo's own plan, not lplan's — those fixes had no CHANGELOG/entity trail. P009 + the new convention give drive-by contributions a one-line-minimum logging path without requiring full WORKFLOW.md onboarding.

2026-08-27 | A016 | NEW → DONE | Tree-view clicks broke on apostrophes in titles (unescaped onclick attribute); master-plan nodes never highlighted and could render twice with duplicate DOM ids. Drive-by fix from superplan's context, backfilled retroactively.

2026-08-27 | A017 | NEW → DONE | Root badges extended to all parentless items; fixed actions with a project but no design vanishing from the tree entirely; page title/toolbar now show the actual repo name; Tree view's Full Doc button no longer dead-ends before Edit. Drive-by fix from superplan's context, backfilled retroactively.

2026-08-27 | A018 | NEW → DONE | Save/Cancel buttons never actually became visible in edit mode (CSS display:none reasserting itself); sidebar toggle glyph never updated (event.currentTarget bug); INDEX.md no longer offers Edit (it's regenerated on every view); repo name now prefers README.md's heading. Drive-by fix from superplan's context, backfilled retroactively.

2026-08-27 | A019 | NEW → DONE | Thesis<->master_plan link now shown in the content pane both directions; toggle-glyph rotation CSS rule removed (conflicted with the text-flip fix); master plans sidebar list now comprehensive, and a master plan's content pane shows its child projects recursively. Drive-by fix from superplan's context, backfilled retroactively.

2026-08-27 | P010, D004, A020-A023 | NEW → PLANNING/IDEA | Plan Health Dashboard scoped at user request: a live, hierarchy-wide status/progress view (rollups + stale/blocked/dangling-ref surfacing) to fill the gap between INDEX.md (static snapshot), Items view (flat table, no rollups), and Analytics dashboard (rollups, Project-only). Planning only — no implementation started.

2026-08-27 | P010, D004, A020, A021, A022 | IN_PROGRESS/IDEA → IN_PROGRESS/DONE | Implemented: new `status_overview.py` (pure rollup/staleness/dangling-ref functions), `/api/status-overview` route, `overview.js` frontend module, new "🩺 Status" toolbar tab. Verified via curl + a standalone script against lplan's own plan/ (matches `plan check-refs` exactly, correctly flags A015/P007/M001 as stale) and the existing 100-test suite still passes. P010 stays IN_PROGRESS (not DONE) and A023 stays IN_PROGRESS: the implementing sandbox had no headless browser, so the rendered UI itself was never visually checked, only its data/wiring.

2026-08-27 | A021 | fix | User tested in a real browser and found row/bar links didn't work — `/api/status-overview` never returned a `path`, so the frontend guessed a filename that didn't match. Fixed by threading `path_by_id` through `status_overview.py`; verified via curl that `/api/file` now resolves every returned path with 200. Exactly the kind of bug the missing-headless-browser caveat was flagging as a risk.

2026-08-27 | A021 | fix | User reported the Projects section got cut off at the bottom with no scrolling. `#overview-view` was missing the `overflow-y: auto` CSS rule its sibling views (`#tree-view`, `#analytics-dashboard`) have — required for it to shrink-to-fit as a flex child of `#content` and actually scroll. Fixed in style.css. Also added collapsible sections (click-to-toggle Needs Attention / Master Plans / Projects headers) since the user asked for scrolling and/or collapsing.

2026-08-27 | WORKFLOW.md, CLAUDE.md | procedure update | New WORKFLOW.md § "Bubbling Up: Maintain the Plan During Work, Not Just After" — codifies five moments plan updates get skipped (task branching, decision points, intermediate checkpoints, task completion, hierarchy propagation) and a one-level-up check table (Action→Design→Project→Master Plan→Thesis). Reason: this session drifted FOCUS.md/CHANGELOG.md days behind reality twice, and P010 (built specifically to catch that) only exists because a human pointed it out — the request was to make agents mindful of plan upkeep *during* work, not just at natural stopping points. CLAUDE.md updated to point here for anyone advancing existing plan work, not just drive-by contributors.

2026-08-27 | WORKFLOW.md | restructure | User flagged the just-added "Bubbling Up" section (and by extension the whole file) as too much text upfront. Split into a short operating-summary `WORKFLOW.md` (693 → 104 lines) with pointers, and a new `WORKFLOW_DETAILS.md` (168 lines) holding the full External Change Detection mechanism, worked examples per change-magnitude level, the Framework Updates/versioning procedure, and extended rationale for Bubbling Up and the External Contribution Workflow. No content dropped, only layered — main file states the rule, details file explains why/how deeper.

2026-08-28 | — | fix | Items view column-header sort never worked (always showed ID order). Cause: the `th[data-column]` click listeners were attached in `attachEventListeners()`, which runs exactly once, before the table (and its `<th>` elements) is ever rendered into the DOM — so `querySelectorAll('th[data-column]')` always found nothing and no listener was ever bound. Moved the listener setup into `attachTableEventListeners()`, which runs after every table render (matching where pagination/row-click listeners already lived). No entity filed — status.js/Items view predates lplan's own entity tracking and isn't scoped under any current project; logged here per Coherence Rule 2 only.

2026-08-30 | WORKFLOW.md, CLAUDE.md | procedure update | Tightened External Contribution Workflow at user request. Reason: 2026-08-30's A024 (external "AI Agent Memory Maintenance" contribution) added a whole new WORKFLOW.md section + template change, filed correctly as paperwork (Action under P009, DONE, CHANGELOG line) but never actually flagged to the maintainer — technically compliant, still landed unreviewed. Split the drive-by convention's middle tier: code bug/layout fixes still go through P009 silently; anything touching WORKFLOW.md/WORKFLOW_DETAILS.md/templates/CLAUDE.md/README.md (or any new entity type/field/behavior) now must be explicitly flagged to the user, never just logged, regardless of size. Added a generalization test ahead of that: propose a policy change only if it would help projects using lplan in general, not just codify one repo's own habits — repo-specific preferences belong in that repo's own plan/WORKFLOW.md instead.

2026-08-30 | docs | distill | User asked to cut root `*.md` and `templates/*.md` in half or more, prioritizing files agents actually load into context. Deleted 4 fully-superseded/stale docs (STATUS_VIEW_PLAN.md, REFACTOR_PLAN.md, TEST_RESULTS.md, MIGRATION.md — completed-feature plans and a shell-script-era migration guide, all git-history-preserved) after explicit confirmation. Compacted `templates/WORKFLOW.md.template` 241→49 lines (copied into every downstream repo, highest-leverage cut) and `DOCS_INDEX.md` 267→33 lines. Trimmed `WORKFLOW.md`'s AI Agent Memory Maintenance section, moving its example/automation-target detail to `WORKFLOW_DETAILS.md` rather than duplicating. Fixed dangling references to the deleted MIGRATION.md in TROUBLESHOOTING.md, QUICK_REFERENCE.md, and WORKFLOW_DETAILS.md; retitled IMPLEMENTATION.md away from its stale "Tier 1"/shell-script framing. QUICK_REFERENCE.md/TROUBLESHOOTING.md/IMPLEMENTATION.md left structurally as-is (not agent-context-loaded; a full distill of those was deferred, not done).

2026-08-30 | — | audit | Ran the newly-added Consumer Repo Structural Alignment Check (A025) against this repo's own `plan/*.md` files, per user request to dogfood it. Found real drift: FOCUS.md had accumulated a duplicate `## Active` heading plus extra sections not in the template; REFLECTION.md had drifted from one-liner format into full markdown sections — the exact "section headers instead of pipe entries" failure D005 exists to prevent, present in lplan's own repo the whole time. Fixed per D005's own convention rather than just reformatting in place: REFLECTION.md restored to one-liners with detail moved to new REFLECTION_extension.md; FOCUS.md's overflow (status breakdown, recent-changes detail) moved to new FOCUS_context.md.

2026-08-30 | D005, A026 | PLANNING/NEW → DONE | Implemented D005 (Template File Family Scaling): new `companions.py` (detection), parser skips companions before parsing (fixes a real bug — any non-frontmatter `.md` dropped into an entity subdirectory previously showed as a hard "parse error"), `generate-index` surfaces companions as a "📎 see also" note plus an optional `--include-companions` inventory section, `check-refs` gained `check_companion_links()` to flag dead companion links (with fenced-code and inline-code guards, both needed — found via dogfooding), template comment blocks added to 8 templates. Dogfooded directly on this repo's own drifted FOCUS.md/REFLECTION.md (see prior entry) plus a real `plan/designs/D005_learnings.md` companion. 100/100 tests pass throughout. Also noted: D005 was filed under P008 (Cross-Repo Planning Integration) whose actual scope doesn't match — left as-is, flagged in P008's own Log rather than moved.

2026-08-30 | M002 | DELETED | Scalability Foundation master plan moved to the accessibility-lplan repo at user request — irrelevant to lplan's own plan. Deleted `plan/master_plans/M002-scalability-foundation.md` and removed it from every live reference: P005's and P008's `parent_master_plan` frontmatter, T001's and T002's Linked § Master Plans, FOCUS.md/FOCUS_context.md's status breakdown, and A023's dogfood note (softened rather than deleted, since it recorded a real historical test result). Left untouched: this CHANGELOG's own 2026-08-24 entry recording M002's creation, and REFLECTION_extension.md's mention of it — both are accurate historical record, not live references, consistent with append-only history not being rewritten.

2026-08-30 | P010, A023 | IN_PROGRESS → DONE | Closed P010's last open item — A023's click-through verification — using `jsdom` since no headless browser was ever available in this environment: fetched the live page from a running `plan serve`, executed the real client-side JS inside a DOM (not a mock), and dispatched genuine click events through the actual listeners. 21/21 checks passed: toolbar tab-switching, real content rendering, the collapsible-section toggle, row-click → EntityViewer content loading via `/api/file`, and confirmation that `#overview-view`'s `overflow-y:auto` CSS rule (A021's scroll fix) is actually applied. P010 marked DONE; M001 (its parent master plan) logged the completion but stays IN_PROGRESS as a standing multi-project vision.

2026-08-30 | P007, A015, D007, P009 | alignment | User asked to align P007 with reality and check A015; found P007 was 7/9 tasks done (impact analysis, Gantt, burndown, live dashboard all built 2026-08-24 through since, never checked off) and A015's dependency-graph SVG genuinely exists (`report.py`'s `_generate_dependency_graph_svg`, verified by regenerating `plan report`) but is static-report-only, never wired into the live "📊 Analytics" dashboard. Wrote D007 (Analytics Architecture) retroactively — P007 and A015 had referenced this design ID since creation but it never existed, which is why `check-refs` had been flagging A015 as orphaned the whole time; now resolved. Fixed two more stale speculative Action-ID references in P007 (A014, A016 — never created / reused elsewhere, same pattern as P008's A017-19). P009: found its `updated` date and Log had never been touched when A024 was added under it 2026-08-27→30 — a real instance of the Bubbling Up gap; fixed.

2026-08-30 | M001 | rewritten | User flagged M001 as looking like test content unrelated to lplan (same fingerprint as M002: generic goals, a fabricated "Q2 2026 survey," no actual connection to lplan) — both were created 2026-08-24 purely to exercise the new MasterPlan entity type. Rather than delete it like M002, rewrote it in place at user's request to be lplan's actual master plan: retitled "lplan Framework Development," vision/scope reset to lplan itself, Tasks restructured into 4 phases mapping the real P001–P010 history plus a Phase 4 (cross-repo maturity: P008, D005 propagation, historical trend tracking). All 5 referencing projects (P004–P007, P010) needed no changes since the ID didn't move. Updated the "Developer Experience" label wherever it appeared as descriptive text (T001–T003, C001, P005) — CHANGELOG's own historical creation entry left untouched per the append-only convention.
2026-08-30 | P001 | DONE → IN_PROGRESS | Reopened for A027 (validator parent-child consistency check). Filed from TradeFlow context: P005 DONE+children IN_PROGRESS/BLOCKED went undetected 9 days.
2026-08-30 | A027 | NEW → IN_PROGRESS | Validator: DONE parent with non-terminal children emits warning in validate_relationships(). Check 1 implemented; check 2 (stale-BLOCKED) deferred.
2026-08-30 | D008 | NEW → PLANNING | Project phase→Action/Design linking. phase frontmatter field on children; generate-index reconstructs phase table; checkbox extraction for in-file completion visibility.
2026-08-30 | D008 | PLANNING (revised) | Loose coupling model: phase text is human-authored, free-form; Design is the minimum anchor per phase; Actions optional. Validator warns on unanchored phases. Project file stays authoritative, not generated.

2026-08-30 | A027 | test coverage | Added 4 test cases in `tests/test_validator.py` at user request for the DONE-parent-with-open-children check: warns on open child Action, warns on BLOCKED child under a DONE Design, silent when all children are terminal, silent/no-crash with zero children. 104→108 tests. Confirmed the check already found a real (expected, non-bug) case in this repo's own plan/: D007 (DONE) has non-terminal child A015 (IN_PROGRESS).

2026-08-30 | D008, A028 | PLANNING → IN_PROGRESS | Implemented D008 Phases 1–3 at user request. Phase 1: optional `phase` field on Design/Action (`models.py`, wired through `parser.py`). Phase 2: new `SchemaValidator.validate_phase_anchors()` — parses a project's raw `## Phases` markdown section (needed the raw body, so it's a separate method from `validate_relationships()`), warns when a phase header has no bracketed ref resolving to a Design; wired into `plan validate`. Phase 3: `generate-index` emits a `## Phase Summaries` section, grouping `phase`-tagged children by project+phase, worst-case status per phase, Actions resolving their project via their parent Design when unset directly. 6 new tests (4 for phase-anchor validation, 2 for the index phase-summary table). Phase 4 (checkbox extraction) left for later per D008's own prioritization. No false positives against lplan's own plan/ — nothing here uses the convention yet, correctly silent (opt-in).

2026-08-30 | P010, A029 | reviewed | User asked to review the Status Overview dashboard for stale assumptions once D008 landed. Found one: `dangling_references()` only ever called `check_references()` — the dashboard's needs-attention panel never ran the full `SchemaValidator`, so A027's and D008's brand-new warning types were invisible in the live dashboard even though `plan validate` showed them. Fixed: new `collect_validator_warnings()` runs the same validator the CLI uses, wired into the dashboard payload and a new "⚠ Validator warnings" panel section in `overview.js`, verified live via jsdom (not just curl). New `tests/test_status_overview.py` — this module had zero test coverage before. P010 stays DONE (small enhancement, not a scope reopening). 114/114 tests pass overall.

2026-08-30 | P010, A030 | reload | User flagged a real math problem: rollup `pct_done` was driven by child Design/Action DONE-counts, but the child set grows as work gets discovered mid-flight (mostly upward), so the percentage drifts away from actual progress. Every project already has a `## Tasks` checkbox section (template-standard, independent of D008) that's the deliberate plan of record instead — user confirmed this applies to Master Plans too (same structure, see M001). Fixed: new `parser.count_checkboxes()` (scans the whole file body, works for both `## Tasks` and D008's `## Phases`) and `status_overview._rollup_pct()`, preferring checkboxes, falling back to child-count, falling back to status — with a new `pct_source` field so the dashboard shows which signal actually drove the number rather than implying they're interchangeable. Immediately found a real issue while dogfooding: P002 and P003 were both DONE with 0% checkbox completion — real, documented work that was simply never checked off. Fixed both files' checkboxes, conservatively (cross-checked each item against the codebase; left 2 of P003's 9 unchecked with no evidence of having been built — Mermaid diagrams, a status-propagation engine). 4 new tests. 118/118 tests pass overall.

2026-08-30 | P010, A031 | added | User asked for the Items view to sort projects by completion, least complete first. `/api/status` now attaches `completion`/`completion_source` to project/master_plan rows via the same `project_rollup()`/`master_plan_rollup()` functions A030 built (not a reimplementation), `status.js` gained a Completion column (bar + %, tooltip when not checkbox-derived) and its default sort changed from `id` to `completion` ascending; the sort comparator was special-cased so entities with no completion measure (design/action/concept/thesis) sort to the end instead of being silently treated as 0%. Verified live via jsdom, filtered to Projects: real order is P008 (0%) → P007 (73%) → P003 (78%) → the rest at 100%.

2026-08-30 | P010, A032 | added | Three follow-ons, all reusing A030's rollup math: (1) Tree view now shows a small muted progress badge after Project/Master Plan titles — `_build_hierarchy()` threads full entity objects through so `completion` can be computed there too. (2) Items view's "COMPLETION" column header renamed to "PROGRESS" (display label only, field name unchanged). (3) The "🩺 Status" dashboard's own project/master-plan rollups now sort least-complete-first (previously by ID) — a separate fix from A031's Items-view sort, since this view still listed by ID. All three verified live via jsdom against a running server. 1 new test (rollup sort order). 119/119 tests pass.

2026-08-30 | P009, A034 | NEW → DONE | Drive-by fix (TradeFlow): validate_phase_anchors() now skips projects with terminal status (DONE/DEFERRED/CANCELLED) — their phase gaps are historical, not actionable. Reduced TradeFlow validate from 14 to 10 warnings.

2026-08-30 | P010, A033 | renamed | User asked to align internal view naming with the toolbar button labels they actually use to refer to views — directly prompted by the ambiguity A031/A032 hit (this repo has two plausibly-"status"-named views). Renamed as a coordinated swap: `overview.js`/`OverviewView` → `status.js`/`StatusView` (button: "🩺 Status"), old `status.js`/`StatusView` → `items.js`/`ItemsView` (button: "📋 Items"), with matching DOM container id and `data-action` string swaps across `dispatcher.js`, `analytics.js`, `files.js`, `tree.js`, and `server.py`'s `<script>` tags. D004/D007 updated where they described the old filenames as current architecture; historical Log entries elsewhere left untouched. Verified live via jsdom: both tabs still show/hide correctly, `window.OverviewView` no longer exists. 119/119 tests pass (Python side untouched — pure JS/HTML rename).

2026-08-30 | A033 | fix | User caught a regression: the Status dashboard stopped scrolling after the rename. Cause: the rename's grep swept `*.js`/`*.py` only and missed `style.css`, which still had `#overview-view { overflow-y: auto; }` targeting an id that no longer existed, while the new `#status-view` had no scroll rule at all — same bug class as A021's original scroll fix, self-inflicted this time. Fixed the one selector; confirmed via jsdom that `#status-view` carries the rule again; grepped `*.css` broadly afterward for any other stale references (none found).

2026-08-31 | P005, D009 | DONE → IN_PROGRESS | User described a real pattern from unstructured work: when projects under a master plan start overlapping/interacting, neither `priority` (absolute tier) nor `depends` (hard blocking) captures "which one do we actually work on first." Designed D009 (Master Plan Priority Stack): optional `priority_stack` field on MasterPlan, an ordered (not exhaustive) list of child Project IDs, advisory only. Two new validator warnings planned: a stack entry that isn't actually a child project, and a stack order that contradicts a hard `depends` relationship. Explicitly scoped out: the equivalent at Project→Design/Action level, which the user also raised — D008's phase model likely already covers that layer, revisit only if a real gap surfaces. Reopened P005 (the project that built MasterPlan) to host this, same pattern as P001 hosting A027/D008; also fixed P005's own stale Linked references (D005/A009/A010 were speculative IDs never created) while in there. Design only — not yet implemented.

2026-08-31 | A035 | NEW → DONE | Found while designing D009: an external merge (`4d31306`) renamed a conflicting file `A033-*.md` → `A034-*.md` but never updated its own `id:` field, leaving two files both claiming `id: A033` — and `plan validate` reported zero problems, because every consumer collapses parsed files into a `{entity.id: entity}` dict where a duplicate just silently overwrites and vanishes. Fixed the immediate collision (corrected the stray file's id to A034) and the structural gap: new `SchemaValidator.validate_unique_ids()`, run first in `plan validate` before anything collapses by ID, errors (not warns) on any ID claimed by more than one file. Reproduced the original bug to confirm the fix actually catches it. 3 new tests. Known gap: only the CLI's `validate` command calls this; other consumers (server.py routes, generate-index) still silently last-write-wins on a duplicate, just without `plan validate` currently surfacing it from those paths — scoped narrowly to the primary integrity entrypoint for now.
